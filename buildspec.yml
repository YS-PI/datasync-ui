version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 24
    commands:
      - npm install

  build:
    commands:
      - npm run build --if-present

artifacts:
  base-directory: dist
  files:
    - '**/*'